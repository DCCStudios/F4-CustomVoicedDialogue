<Window x:Class="CustomVoicedDialogue.App.PromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Direct this line" Height="360" Width="560"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize"
        Background="#FF1E1E24" Foreground="#FFE8E8E8">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFE8E8E8"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="MinWidth" Value="90"/>
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="LineText" FontWeight="Bold" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="1" Opacity="0.75" FontSize="11" Margin="0,0,0,8"
                   Text="Describe how you want it performed, the way you would tell a voice actor — &quot;exhausted, almost out of breath&quot;, &quot;quiet and dangerous, no anger&quot;, &quot;laughing halfway through&quot;. The spoken words never change; only the delivery does."/>

        <StackPanel Grid.Row="2">
            <TextBox x:Name="DirectionBox" AcceptsReturn="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" MinHeight="72"
                     Background="#FF17171C" Foreground="#FFE8E8E8" CaretBrush="#FFE8E8E8"
                     BorderBrush="#FF3A3A45" Padding="6"/>
            <TextBlock Opacity="0.6" FontSize="11" Margin="0,6,0,0" TextWrapping="Wrap"
                       Text="Write plain words and the model shapes them into direction. Write your own [bracket] and it is used exactly as typed (the character voice still applies). Put *nv* outside the brackets (e.g. [sigh] *nv*) to drop the spoken words — only the bracket is performed. Put *ph* outside the brackets when the line itself is an onomatopoeic sound (like the game's 'Nnyyyaaarrgghh!') — it translates that spelling into phonetic sounds the synthesizer can actually voice, since it can't read the raw letters. Write exact-text inside the bracket (e.g. [exact-text weary, low]) to send your bracket verbatim with nothing auto-added. Regenerate re-rolls with the normal rules; reopen Direct to use your directed text again."/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="OkButton" Content="Generate ▶" IsDefault="True" Click="OnOk"/>
            <Button Content="Cancel" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
